# Copy this file to .env and fill in your details
# The .env file is in .gitignore and won't be committed to version control

# Required: Get your Pushbullet token from https://www.pushbullet.com/#settings/account
PUSHBULLET_TOKEN=your_pushbullet_token_here

# Optional: Set a custom path for the config file (default: price_tracker_config.json in the current directory)
# CONFIG_FILE=~/.config/price_tracker/config.json

# GitHub Token (only needed if you want to sync with GitHub)
# GITHUB_TOKEN=your_github_token_here
